---
label: public transport
hide_body: true
fields:
- name: the_case_for
  type: text
  config:
    required: false
  label: the case for
- name: footprints
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: footprints
  description: mass transit systems are more space efficient.
